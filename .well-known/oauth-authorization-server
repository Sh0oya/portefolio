{
  "$comment": "OAuth 2.0 Authorization Server Metadata (RFC 8414) for mathieuhaye.fr. This site exposes ONLY public, read-only resources: there is no interactive OAuth flow, no token endpoint, and no agent-registration backend. This document exists to (a) carry the 'agent_auth' discovery block referenced by /auth.md and (b) state, in machine-readable form, that anonymous access is the only supported mode. If a protected API is added later, real authorization_endpoint, token_endpoint, register_uri, claim_uri and revocation_uri values will be published here.",
  "issuer": "https://mathieuhaye.fr",
  "service_documentation": "https://mathieuhaye.fr/auth.md",
  "authorization_servers": [],
  "scopes_supported": [],
  "response_types_supported": [],
  "grant_types_supported": [],
  "token_endpoint_auth_methods_supported": ["none"],
  "agent_auth": {
    "skill": "https://mathieuhaye.fr/auth.md",
    "registration_required": false,
    "identity_types_supported": ["anonymous"],
    "anonymous": {
      "credential_types_supported": []
    },
    "$comment": "Public read-only resource. No register_uri, claim_uri or revocation_uri are published because no credential is ever issued. Anonymous GET over HTTPS is the only access mode."
  }
}
